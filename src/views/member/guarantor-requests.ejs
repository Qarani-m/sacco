<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - SACCO</title>
    <link rel="stylesheet" href="/css/design-system.css">
</head>
<body>
    <%- include('../layouts/member-header') %>

    <div class="container-xl">
        <!-- Page Header -->
        <div class="page-header">
            <div class="accent-line"></div>
            <h1 class="page-title">Guarantor Requests</h1>
            <p class="page-subtitle">Manage loan guarantor requests from other members</p>
        </div>

        <!-- Pending Requests -->
        <% if (pendingRequests && pendingRequests.length > 0) { %>
        <div class="card mb-6" style="background: #FEF2F2; border: 2px solid #000;">
            <h3 class="small-title" style="margin-bottom: 1.5rem;">⏳ Pending Requests (<%= pendingRequests.length %>)</h3>

            <div style="display: grid; gap: 1.5rem;">
                <% pendingRequests.forEach(request => { %>
                <div style="padding: 1.5rem; background: #FFF; border: 2px solid #000;">
                    <div class="grid grid-cols-2 gap-6">
                        <!-- Request Details -->
                        <div>
                            <h4 style="font-weight: 700; font-size: 1.125rem; margin-bottom: 1rem;">
                                <%= request.borrower_name %>
                            </h4>
                            <div style="display: grid; gap: 0.75rem;">
                                <div style="display: flex; justify-content: space-between;">
                                    <span class="text-meta">Loan Amount:</span>
                                    <span style="font-weight: 600;">KSh <%= request.requested_amount.toLocaleString() %></span>
                                </div>
                                <div style="display: flex; justify-content: space-between;">
                                    <span class="text-meta">Repayment Period:</span>
                                    <span style="font-weight: 600;"><%= request.repayment_months %> months</span>
                                </div>
                                <div style="display: flex; justify-content: space-between;">
                                    <span class="text-meta">Shares Requested:</span>
                                    <span style="font-weight: 700; color: #DC2626;"><%= request.shares_pledged %> shares</span>
                                </div>
                                <div style="display: flex; justify-content: space-between;">
                                    <span class="text-meta">Amount Covered:</span>
                                    <span style="font-weight: 700; color: #DC2626;">KSh <%= request.amount_covered.toLocaleString() %></span>
                                </div>
                            </div>
                        </div>

                        <!-- Response Form -->
                        <div>
                            <form class="guarantor-response-form" data-request-id="<%= request.id %>" style="display: grid; gap: 1rem;">
                                <div>
                                    <label class="form-label">Adjust Shares (Optional)</label>
                                    <input
                                        type="number"
                                        name="shares_pledged"
                                        class="form-input"
                                        min="1"
                                        max="<%= request.shares_pledged %>"
                                        value="<%= request.shares_pledged %>"
                                        style="border: 2px solid #000; font-weight: 600;"
                                    >
                                    <p class="text-meta" style="margin-top: 0.5rem;">
                                        You can reduce the number of shares before accepting
                                    </p>
                                </div>

                                <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 0.75rem;">
                                    <button
                                        type="button"
                                        class="btn-accept"
                                        onclick="respondToRequest('<%= request.id %>', 'accept')"
                                        style="background: #000; color: #FFF; border: 2px solid #000; padding: 0.75rem; font-weight: 600;"
                                    >
                                        ✓ Accept
                                    </button>
                                    <button
                                        type="button"
                                        class="btn-decline"
                                        onclick="respondToRequest('<%= request.id %>', 'decline')"
                                        style="background: #FFF; color: #DC2626; border: 2px solid #DC2626; padding: 0.75rem; font-weight: 600;"
                                    >
                                        ✗ Decline
                                    </button>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
                <% }); %>
            </div>
        </div>
        <% } else { %>
        <div class="card mb-6" style="background: #F0FDF4; border: 2px solid #000;">
            <div style="text-align: center; padding: 2rem;">
                <p style="font-size: 3rem; margin-bottom: 1rem;">✓</p>
                <h3 style="font-weight: 600; margin-bottom: 0.5rem;">No Pending Requests</h3>
                <p class="text-meta" style="margin: 0;">You don't have any pending guarantor requests at the moment</p>
            </div>
        </div>
        <% } %>

        <!-- Request History -->
        <div class="card">
            <h3 class="small-title" style="margin-bottom: 1.5rem;">📜 Request History</h3>

            <% if (allRequests && allRequests.length > 0) { %>
            <table style="width: 100%; border-collapse: collapse;">
                <thead style="background: #000; color: #FFF;">
                    <tr>
                        <th style="padding: 1rem; text-align: left; border: 2px solid #000;">Borrower</th>
                        <th style="padding: 1rem; text-align: right; border: 2px solid #000;">Loan Amount</th>
                        <th style="padding: 1rem; text-align: right; border: 2px solid #000;">Shares</th>
                        <th style="padding: 1rem; text-align: center; border: 2px solid #000;">Status</th>
                        <th style="padding: 1rem; text-align: center; border: 2px solid #000;">Loan Status</th>
                        <th style="padding: 1rem; text-align: right; border: 2px solid #000;">Date</th>
                    </tr>
                </thead>
                <tbody>
                    <% allRequests.forEach((request, index) => { %>
                    <tr style="<%= index % 2 === 0 ? 'background: #FAFAFA;' : 'background: #FFF;' %>">
                        <td style="padding: 1rem; border: 2px solid #E5E5E5; font-weight: 600;">
                            <%= request.borrower_name %>
                        </td>
                        <td style="padding: 1rem; text-align: right; border: 2px solid #E5E5E5;">
                            KSh <%= request.requested_amount ? request.requested_amount.toLocaleString() : 'N/A' %>
                        </td>
                        <td style="padding: 1rem; text-align: right; border: 2px solid #E5E5E5; font-weight: 600;">
                            <%= request.shares_pledged %>
                        </td>
                        <td style="padding: 1rem; text-align: center; border: 2px solid #E5E5E5;">
                            <% if (request.status === 'accepted') { %>
                                <span style="padding: 0.5rem 1rem; background: #10B981; color: #FFF; border-radius: 4px; font-weight: 600;">Accepted</span>
                            <% } else if (request.status === 'declined') { %>
                                <span style="padding: 0.5rem 1rem; background: #EF4444; color: #FFF; border-radius: 4px; font-weight: 600;">Declined</span>
                            <% } else if (request.status === 'released') { %>
                                <span style="padding: 0.5rem 1rem; background: #6B7280; color: #FFF; border-radius: 4px; font-weight: 600;">Released</span>
                            <% } else { %>
                                <span style="padding: 0.5rem 1rem; background: #F59E0B; color: #FFF; border-radius: 4px; font-weight: 600;">Pending</span>
                            <% } %>
                        </td>
                        <td style="padding: 1rem; text-align: center; border: 2px solid #E5E5E5;">
                            <% if (request.loan_status === 'active') { %>
                                <span style="color: #10B981; font-weight: 600;">Active</span>
                            <% } else if (request.loan_status === 'paid') { %>
                                <span style="color: #6B7280; font-weight: 600;">Paid</span>
                            <% } else if (request.loan_status === 'pending') { %>
                                <span style="color: #F59E0B; font-weight: 600;">Pending</span>
                            <% } else { %>
                                <span style="color: #6B7280; font-weight: 600;"><%= request.loan_status || 'N/A' %></span>
                            <% } %>
                        </td>
                        <td style="padding: 1rem; text-align: right; border: 2px solid #E5E5E5;">
                            <%= new Date(request.created_at).toLocaleDateString() %>
                        </td>
                    </tr>
                    <% }); %>
                </tbody>
            </table>
            <% } else { %>
            <div style="text-align: center; padding: 2rem; background: #FAFAFA; border: 2px solid #E5E5E5;">
                <p class="text-meta" style="margin: 0;">No request history</p>
            </div>
            <% } %>
        </div>
    </div>

    <script>
        async function respondToRequest(requestId, action) {
            const form = document.querySelector(`form[data-request-id="${requestId}"]`);
            const sharesPledged = form.querySelector('input[name="shares_pledged"]').value;

            if (!confirm(`Are you sure you want to ${action} this guarantor request?`)) {
                return;
            }

            try {
                const response = await fetch(`/members/guarantor-requests/${requestId}/respond`, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                        'CSRF-Token': '<%= csrfToken %>'
                    },
                    body: JSON.stringify({
                        action,
                        shares_pledged: parseInt(sharesPledged)
                    })
                });

                const data = await response.json();

                if (data.success) {
                    alert(data.message);
                    window.location.reload();
                } else {
                    alert(data.error || 'Failed to respond to request');
                }
            } catch (error) {
                console.error('Response error:', error);
                alert('Failed to respond to request. Please try again.');
            }
        }
    </script>
</body>
</html>
