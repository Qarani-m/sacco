<div class="container" style="text-align: center; padding: 50px;">
    <h1 style="color: #ef4444; margin-bottom: 20px;">500 - Server Error</h1>
    <p style="font-size: 1.2em; color: #374151; margin-bottom: 30px;">
        <%= typeof message !== 'undefined' ? message : 'Something went wrong on our end.' %>
    </p>
    <% if (error && Object.keys(error).length > 0) { %>
        <pre style="text-align: left; background: #f3f4f6; padding: 1rem; border-radius: 4px; overflow-x: auto;"><%= error.stack %></pre>
    <% } %>
    <a href="/" class="btn btn-primary" style="display: inline-block; margin-top: 20px; background: #000; color: white; padding: 0.5rem 1.5rem; text-decoration: none; border-radius: 4px;">Go Home</a>
</div>
