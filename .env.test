# Test Environment Variables

NODE_ENV=test
PORT=3001

# Test Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=sacco_test
TEST_DB_NAME=sacco_test

# JWT
JWT_SECRET=test-jwt-secret-key-for-testing-only
SESSION_SECRET=test-session-secret-for-testing-only

# M-Pesa (Test Credentials)
MPESA_CONSUMER_KEY=test_key
MPESA_CONSUMER_SECRET=test_secret
MPESA_SHORTCODE=test_shortcode
MPESA_PASSKEY=test_passkey
MPESA_CALLBACK_URL=http://localhost:3001/mpesa/callback

# Email (Test - Use ethereal or mailtrap)
EMAIL_HOST=smtp.ethereal.email
EMAIL_PORT=587
EMAIL_USER=test@ethereal.email
EMAIL_PASS=test_password
EMAIL_FROM=noreply@saccotest.com

# Application
APP_URL=http://localhost:3001
REGISTRATION_FEE=500
